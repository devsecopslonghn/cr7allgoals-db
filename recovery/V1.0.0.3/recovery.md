# Recovery: V1.0.0.3

Recovery classification: FIX_FORWARD  
Application rollback compatible: YES  
Data loss: NO  
DBA intervention: only if dependent objects prevent replacement

The view can be replaced by a later `CREATE OR REPLACE VIEW` migration.
